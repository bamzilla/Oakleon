(function(React, module, undefined) {
  module.exports = React.createClass({
    render: function() {
      return (
        <div className="alert alert-info">
          <strong>Demo test! </strong>Add an artist and song. Rate the song and delete.
        </div>
      );
    }
  });
}(React, module));

